# Configuration pour le système de Prévision des Salaires
# Ajouter ces variables à votre fichier .env ou .env.local

# ====================================
# CRON JOB SECRET
# ====================================
# Clé secrète pour sécuriser l'endpoint cron
# Générer une clé forte: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your-super-secret-key-min-32-characters

# ====================================
# CONFIGURATION SMTP (OPTIONNEL)
# ====================================
# Si non configuré, utilise Ethereal email pour les tests

# Gmail (exemple)
# 1. Activer 2FA sur Google Account
# 2. Générer une "App Password": https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Brevo/Sendinblue (exemple)
# SMTP_HOST=smtp-relay.brevo.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-api-key

# Mailgun (exemple)
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@mg.example.com
# SMTP_PASS=your-mailgun-password

# Email "From"
SMTP_FROM=noreply@kekeligroup.com

# ====================================
# CONFIGURATION NOTIFICATIONS
# ====================================
# Jours avant le paiement pour envoyer la notification
SALARY_NOTIFICATION_DAYS_BEFORE=5

# ====================================
# CONFIGURATION CRON
# ====================================
# Heure du cron job (format 24h)
SALARY_CRON_HOUR=9

# ====================================
# NOTES
# ====================================
# 1. CRON_SECRET: Requis pour sécuriser les endpoints de cron
# 2. SMTP_*: Optionnel. Sans configuration, utilise Ethereal (dev only)
# 3. Les tarifs horaires sont configurés via /admin/salary-settings
# 4. Les timesheets doivent être validés pour calculer les prévisions
# 5. Les notifications sont envoyées 5 jours avant le dernier jour du mois

# ====================================
# EXEMPLES DE VALEURS
# ====================================

# CRON_SECRET (généré automatiquement)
# CRON_SECRET=a3f8e1c7d4b9e2f5a6c8d1e3f4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1

# SMTP pour Gmail
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=mon-email@gmail.com
# SMTP_PASS=monmotdepasse123xyz
# SMTP_FROM=mon-email@gmail.com

# SMTP pour Outlook/Office365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=mon-email@company.com
# SMTP_PASS=monmotdepasse123xyz
# SMTP_FROM=mon-email@company.com
